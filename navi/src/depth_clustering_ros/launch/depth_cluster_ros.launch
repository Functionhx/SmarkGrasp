<launch>
    
    <!--- Sim Time -->
<!--    <param name="/use_sim_time" value="true" />-->

    <!-- <node pkg="rviz" type="rviz" name="rviz" args="-d $(find depth_cluster_ros)/rviz/simple_rviz.rviz" /> -->
    <rosparam command="load" file="$(find depth_cluster_ros)/config/cube.yaml" />
    <node pkg="depth_cluster_ros" type="depth_cluster_node"    name="depth_cluster_node" respawn="true"   output="screen"/>

</launch>
